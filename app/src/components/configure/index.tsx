import * as React from 'react'
import * as types from '../../store/types'

type props = {
}

type state = {
}

export default class Configure extends React.Component<props, state> {
    constructor(props) {
        super(props)
        this.state = {

        }
    }

    render() {
        return <div className='panel panel-body'>
            Configure
        </div>
    }
}